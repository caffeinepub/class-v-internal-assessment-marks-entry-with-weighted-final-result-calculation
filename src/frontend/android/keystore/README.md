# Release Signing Configuration

This directory is intended for documentation about release signing. **DO NOT** commit keystore files to version control.

## Generating a Release Keystore

To create a keystore for signing release builds:

